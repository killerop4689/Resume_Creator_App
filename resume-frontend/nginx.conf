server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # React Router: serve index.html for any unmatched route
    # so refreshing /history/5 doesn't 404
    location / {
        try_files $uri $uri/ /index.html;
    }
}